/** Small label for fields that are not required. */
export function OptionalFieldTag() {
  return (
    <span className="text-[10px] font-semibold uppercase tracking-wider text-slate-400">
      Optional
    </span>
  );
}
